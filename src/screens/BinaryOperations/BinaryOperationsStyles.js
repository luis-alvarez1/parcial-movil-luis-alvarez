import { StyleSheet } from 'react-native';

export const binaryOperationsStyles = StyleSheet.create({
  radioItem: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  selectionContainer: {
    marginTop: 20,
  },
});
