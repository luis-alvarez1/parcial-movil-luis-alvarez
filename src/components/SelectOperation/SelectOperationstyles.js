import { StyleSheet } from 'react-native';

export const selectOperationstyles = StyleSheet.create({
  radioItem: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  selectionContainer: {
    marginTop: 20,
  },
});
